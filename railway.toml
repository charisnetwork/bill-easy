[build]
builder = "nixpacks"

# Force rebuild: cache-bust-2026-05-11T18-15-00Z

[phases.build]
cmds = ["echo 'Building with cache bust'"]

[phases.setup]
nixPkgs = ["nodejs_22"]

[start]
cmd = "echo 'Gateway starting on port 3000' && node railway-monorepo.js"

[deploy]
startCommand = "echo 'Gateway starting on port 3000' && node railway-monorepo.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
healthcheckPath = "/health"
healthcheckTimeout = 100
