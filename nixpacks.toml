[phases.setup]
nixPkgs = ["nodejs_22", "npm"]

[phases.install]
cmds = ["npm install", "cd backend && npm install", "cd admin/backend && npm install"]

[phases.build]
cmds = ["echo 'Build phase complete'"]

[start]
cmd = "node railway-monorepo.js"
